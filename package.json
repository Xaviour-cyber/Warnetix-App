{
  "name": "warnetix-root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "postinstall": "npm ci --prefix frontend/public/src || npm install --prefix frontend/public/src",
    "build": "npm run build --prefix frontend/public/src",
    "start": "serve -s frontend/public/src/dist -l $PORT"
  },
  "dependencies": {
    "serve": "14.2.3"
  }
}
