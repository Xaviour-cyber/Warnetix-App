{
  "name": "warnetix-root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "postinstall": "cd frontend/public/src && (npm ci --no-audit --no-fund || npm install --no-audit --no-fund)",
    "build": "cd frontend/public/src && npm run build",
    "start": "npx serve -s frontend/public/src/dist -l $PORT"
  },
  "dependencies": {
    "serve": "14.2.3"
  }
}
